"""Voice Wheel / AI Voice Clipboard.

Hold a key, speak, pick a mode around the cursor, release — a finished,
paste-ready result is in the clipboard. See README.md and the plan for the
staged build (Phase 0 spike → A core → B LLM → C context → D wheel).
"""

__version__ = "0.1.0"
