# Voice Wheel — Фичи

Порядок = последовательность внедрения (с учётом зависимостей).
Статусы: ✅ готово · 🟡 частично · ⬜ не начато

## ✅ Сделано

| № | Фича | Описание | Статус |
|---|------|----------|--------|
| 1 | Триггер — боковая кнопка мыши | Запись идёт, пока зажата боковая кнопка (#3); колесо появляется у курсора | ✅ |
| 2 | Колесо-оверлей без кражи фокуса | Полупрозрачное колесо у курсора, не уводит фокус — результат вставляется в активное приложение | ✅ |
| 3 | Центр = диктовка | Отпустил в центре → транскрибация сразу в буфер, без LLM | ✅ |
| 4 | STT mlx-whisper | Быстрая локальная транскрибация (ru) на Apple Silicon | ✅ |
| 5 | Сектора = обработка LLM | Отпустил в секторе → текст прогоняется через промпт сектора → в буфер | ✅ |
| 6 | Промпты = папка .md → авто-секции | Колесо строит сектора по числу .md-файлов в `prompts/` (`N-Название.md`); правишь файлы — меняешь сектора (рестарт применяет). 4 дефолта в комплекте | ✅ |
| 7 | Локальный LLM (Ollama) | qwen2.5:7b локально, ~2с, бесплатно, без ключа, без лимитов; keep_alive держит модель в RAM | ✅ |
| 8 | Переключаемые LLM-бэкенды | ollama / claude_warm / claude_cli / anthropic — выбор в config.json | ✅ |
| 9 | Подавление «назад» на кнопке | Кнопка-триггер не листает браузер назад | ✅ |
| 10 | Импульс у курсора (цветной) | Вспышка у курсора при готовности: 🟢 ок · 🟠 сырой текст · 🔴 провал | ✅ |
| 11 | Прогресс + защита от потери текста | Спиннер у курсора, пока идёт обработка; при сбое LLM в буфер кладётся сырая расшифровка (текст не теряется) | ✅ |
| ✦ | Cyberpunk-дизайн (колесо + спиннер) | Неоновый стиль: тёмная база, цвет на сектор (без подписей по кольцу), микрофон в центре, активный сектор → его название по центру (+БУФЕР во внешнем), кольцевые сегменты, компактнее | ✅ |
| ✦ | Гибкий триггер (любое железо) | Любая кнопка/клавиша на любое действие: mouse_side / keyboard / mouse — для мышей без боковых кнопок | ✅ |
| ✦ | Озвучка (TTS) — переключаемый бэкенд | Кнопка #4 читает буфер вслух, офлайн. `tts.backend`: `system` (голоса macOS, авто-подбор лучшего качества) или `piper` (локальный нейро-голос, авто-загрузка; ru: Irina/Денис/Руслан/Дмитрий). Выбор в окне настроек. Повторное нажатие — стоп | ✅ |
| ✦ | Модель на каждый промпт | `config.json → sector_models`: для каждого сектора свой backend+model (можно дешёвую локальную на одно, платную облачную на другое). Фолбэк — дефолтный `llm` | ✅ |
| ✦ | Запись во время обработки | Тумблер `config.json → concurrent`. Вкл: можно зажать и записать новое, пока прошлое ещё обрабатывается (результаты перетирают, последний побеждает). Выкл (дефолт): новое нажатие сбрасывает текущее | ✅ |
| ✦ | ~~Вернуть прошлый буфер~~ (убрано) | Заменено Историей: зашёл в подменю → кликнул нужный результат → скопировался. Отдельная кнопка убрана как избыточная | ➖ |
| ✦ | Быстрый запуск + .app | `run.sh` (поднимает Ollama, чистый рестарт) и `make_app.sh` → double-click «Voice Wheel.app» из Finder (LSUIElement, стабильный bundle-id для прав). Запуск без Accessibility больше не крашит — живёт и просит доступ | ✅ |
| ✦ | Отмена жестом | Увёл курсор за внешнее кольцо → центр показывает «✕ Отмена» (красным); отпустил там — запись сбрасывается, ничего не обрабатывается | ✅ |

## ✅ Дорожная карта v1 (внедрено)

| № | Фича | Описание | Зависит от | Статус |
|---|------|----------|------------|--------|
| 12 | 2-е кольцо = контекст из буфера | Внешнее кольцо: буфер как контекст + голос-инструкция → промпт сектора → в буфер. Центр=диктовка, внутр=обработать речь, внешн=ответить на буфер | 6 | ✅ |
| 13 | Документация запуска из консоли | README: установка, запуск, смена бэкенда, правка промптов, триггер, troubleshooting — без GUI | 6 | ✅ |
| 14 | Разделение core / hostos | `core/` (config, modes, pipeline, llm, stt, recorder, history, job_tracker — без ОС) и `hostos/macos/` (overlay, tap, tray, tts, clipboard, settings, app; пакет назван `hostos`, чтобы не тенить stdlib `platform`). Вход `python -m voice_wheel` выбирает платформу. Ядро не зависит от платформы → Windows добавляется рядом | — | ✅ |
| 15 | STT переключаемый (faster-whisper) | `stt.backend`: `auto` (mlx на Apple Silicon, иначе faster-whisper) / `mlx` / `faster-whisper`. Выбор в окне настроек. faster-whisper установлен и проверен end-to-end (распознаёт точно). Кроссплатформенный движок готов | 14 | ✅ |
| 17 | Трей-иконка | Иконка в меню-баре (NSStatusItem): цвет = статус (🟢 готов / 🔴 не готов: прогрев или нет Accessibility), История, Настройки, Выход. Текстовая строка статуса убрана — цвет иконки информативнее и виден всегда | — | ✅ |
| 18 | История результатов (просмотр) | Подменю «История» в трее: последние 10 результатов; клик = заново скопировать в буфер | 17 | ✅ |
| 19 | Окно настроек | Нативное окно (меню-бар → Настройки…): backend, модели, язык, триггеры, тумблеры → пишет config.json | 17 | ✅ |
| 20 | LLM-режимы в настройках | Выбор backend (ollama/claude_warm/anthropic/claude_cli) + модели прямо в окне настроек | 19 | ✅ |

## ⬜ Планируется (выписываем сюда → реализуем по порядку)

| № | Фича | Описание | Зависит от | Статус |
|---|------|----------|------------|--------|
| 23 | Озвучка **выделенного** текста | Нажал кнопку озвучки → если что-то **выделено** (в любом приложении) — читает выделенное (приоритет над буфером); если выделения нет — читает буфер. Кросс-апп: на фоновом потоке эмулируем Cmd+C, по `changeCount` ловим выделение, **буфер возвращаем как был**. Без ручного Ctrl+C. Live-проверка в Telegram — за пользователем | TTS | ✅ |
| 24 | Первый запуск: авто-загрузка голоса | Piper — **дефолтный** бэкенд. Голос качается **в прогреве** (фон): «Готовлю голос Piper (~60 МБ)…» → «готов»; иконка красная пока качается. Если не скачался (офлайн) → **fallback на системный голос macOS**, без падения. Проверено на чистом кэше (качает сам) и на битом голосе (graceful) | Piper TTS | ✅ |
| 25 | SETUP.md — что нужно установить | `SETUP.md`: Python 3.12 + venv (вкл. трюк с путём-с-двоеточием), зависимости, Ollama + модель, права macOS, что качается само (Whisper, Piper-голос) и что вручную, проверка, troubleshooting | — | ✅ |
| 26 | Переписать README (англ. база) | README переписан: актуальный вход (`python -m voice_wheel`), верная раскладка (core/hostos), TTS/настройки/трей, ссылки на SETUP/SETTINGS/ARCHITECTURE. Убран мёртвый текст (старый `macos_app`, спайк). Русская версия — опц., позже | — | ✅ |
| 27 | Редизайн окна настроек (группировка) | Сгруппировано по блокам (🧠 LLM / 🎙 STT / 🔊 Голос / ⌨️ Триггеры / ⚙️ Прочее), человеческие подписи бэкендов («Локально — Ollama (бесплатно)» вместо `ollama`), поле модели скрывается по контексту (Ollama-модель при Ollama, Claude-модель при Claude), серые подсказки | 19 | ✅ |
| 28 | ~~Настройки v2~~ (разбита на #36–#42) | Большой замысел из `SETTINGS.md` разбит на отдельные задачи ниже (по просьбе: «делаем всё в рамках разных задач») | — | ➗ |
| 36 | LLM: три типа подключения (радио) | ✅ Вместо дропдауна движков — **3 радио**: **Прямой API** (провайдер Anthropic/OpenAI + ключ + модель) / **Ollama** (модель + URL) / **Claude Code** (модель). Видно только настройки выбранного типа (остальные группы свёрнуты через `setHidden_`). Тип ↔ бэкенд: api→anthropic/openai, ollama→ollama, cc→claude_warm. `_conn_type_of` мапит загруженный бэкенд на радио; save мапит обратно. Правила-оверрайды (гранулярный дропдаун из 5) не тронуты; «+ Добавить» дефолтит на текущий тип. Полный путь к `claude`/топ-модели/проверка Ollama — отдельно (#39/#40) | 27 | ✅ |
| 37 | LLM: бэкенд OpenAI | ✅ Бэкенд `openai` в `core/llm.py`: Chat Completions, ключ из `OPENAI_API_KEY` (грузится из `.env`), ленивый клиент с guard'ом импорта, прогрев в `warm_up`, prompt caching у OpenAI автоматический. Доступность по наличию ключа. Уже **выбирается** в дропдауне движков (+ в правилах-оверрайдах), строка модели стала общей «Модель» (Claude+OpenAI). Зависимость `openai>=1.40` в requirements/pyproject. Тесты с фейковым клиентом (без сети). Полноценный UI «Прямой API» (провайдер-радио + поле ключа) — в #36/#38 | 36 | ✅ |
| 38 | LLM: хранение API-ключа | ✅ Решено: **`.env`** (в .gitignore). Поле «API-ключ» в группе «Прямой API»; читается из `.env` по env-var провайдера (`ANTHROPIC_API_KEY`/`OPENAI_API_KEY`), при смене провайдера перечитывается. На «Сохранить» пишется в `.env` (обновляет/добавляет строку, остальные сохраняет) **и в `os.environ`** — чтобы live-apply LLM-клиент подхватил ключ сразу, без рестарта | 36 | ✅ |
| 39 | LLM: топ-модели + «Скачать» | ✅ Поля модели стали **editable-комбобоксами** (`NSComboBox`): в дропдауне — топ-рекомендации, но можно вписать свою. Рекомендации зависят от типа: Ollama (`qwen2.5:7b/3b`, `llama3.1:8b`…), Anthropic (`claude-haiku/sonnet-4-5`), OpenAI (`gpt-4o-mini/4o`), Claude Code. У «Прямого API» список переключается при смене провайдера (+ ставит дефолт). У Ollama — кнопка **«Скачать»** → `ollama pull <model>` в фоне, прогресс/итог в ноте, скачанная модель добавляется в дропдаун. Dirty-трекинг комбобоксов (typing + выбор из списка) | 36 | ✅ |
| 40 | LLM: проверка/установка Ollama | ✅ В группе Ollama — **статус-строка** (проверяется в фоне при выборе типа): 🟢 запущена / 🟠 установлена, но не запущена / 🔴 не установлена (`shutil.which` + `/usr/local`,`/opt/homebrew` + пинг `…/api/tags`). Контекстная кнопка: «Установить Ollama…» (→ ollama.com/download) если нет, «Запустить Ollama» (`ollama serve` детачем + авто-перепроверка) если не запущена, скрыта если работает. Кнопка «Проверить» для ручного обновления. **Решение:** только ссылка на установку (без тихой авто-установки — требует прав/brew), но запуск одной кнопкой есть | 36 | ✅ |
| 41 | Настройки: тултипы | ✅ Короткие подсказки при наведении (`setToolTip_`) на ~23 контрола: радио-типы подключения, провайдер, ключ, модели, URL/«Скачать», STT-движок/модель/язык, озвучка/голос/премиум, триггеры (колесо+озвучка) и «Поймать», concurrent, язык интерфейса. Строки в `i18n` (`tip_*`, RU/EN) → переводятся вместе с окном. Один `_set_tooltips()` в конце `_build` | 27 | ✅ |
| 42 | Онбординг первого запуска | ✅ При первом запуске (нет `config.json`) — приветственный `NSAlert` через 1с после старта: 3 шага (выбери способ → выбери/скачай модель → Сохранить) + кнопка «Открыть настройки» (открывает окно). До настройки приложение не падает — распознавание работает, LLM просто фолбэчит на сырой текст. Текст в `i18n` (RU/EN). ⚠️ Заодно: heartbeat-таймер переведён в `NSRunLoopCommonModes`, чтобы пережить модальный цикл алерта (иначе heartbeat застывал бы и супервайзер убил бы приложение) | — | ✅ |
| 43 | Английский язык интерфейса | ✅ Строки централизованы в `hostos/macos/i18n.py` (`STR` RU/EN + `t()` + детект `NSLocale`), используются и окном настроек, и **меню-баром**. Хранится в `config.json` → `ui_language` (`""` = по системе). **Смена применяется на «Сохранить» → приложение перезапускается** (`os.execv`), чтобы всё приложение (меню История/Настройки/Выход, ready-строка, окно) поднялось на новом языке — раньше менялось только окно, меню оставалось на старом. На вкладке «Язык» — предупреждение о перезапуске. Дропдауны с переводимыми подписями (движки LLM, голоса TTS) маппятся по **индексу**, а не по тексту — смена языка не ломает «Сохранить». Технические токен-дропдауны (auto/mlx, mouse_side) не переводятся | 36, 41 | ✅ |
| 45 | Редизайн окна настроек (Auto-Layout) | ✅ Раскладка переписана с ручных `NSMakeRect` на **NSStackView/Auto-Layout**: подпись (150px) + контрол в горизонтальном стеке → колонки **выравниваются сами**, popup'ы не режутся, скрытая строка модели сворачивается без дыры. Поведения (скрытие модели, дизейбл TTS, edit-buffer, capture, превью) целы. (LLM-модель «база+правила» — отдельно #46) | 35 | ✅ |
| 46 | LLM-вкладка: базовая + правила-оверрайды | ✅ **Базовая** (движок+модель) — дефолт для всех. Ниже — **список правил**: каждое = [промпт ▾] [движок ▾] [модель] [✕]. **«+ Добавить правило»** дефолтит на базовую, гаснет когда у всех промптов есть правило. Данные: база → `llm`, правила → `sector_models`. Заменил блок #33. ⚠️ **Скролл** при многих правилах — отдельный follow-up (#55); при 4 дефолтных промптах влезает | 35, 45 | ✅ |
| 55 | LLM-правила: скролл | ✅ LLM-вкладка обёрнута в `NSScrollView` (flipped document-view, ширина пришпилена к clip-view, высота растёт по контенту, авто-скроллер по вертикали). Когда правил/контента больше, чем влезает — появляется вертикальный скролл вместо обрезки. `add_tab(scroll=True)` только для LLM; остальные вкладки короткие, без скролла. Из #46 | 46 | ✅ |
| 48 | Watchdog: авто-рестарт при зависании | ✅ Heartbeat: main-таймер пишет файл каждую секунду. `run.sh` стал **супервайзером**: следит за процессом и свежестью heartbeat — если main-поток завис (>15с без heartbeat) или процесс упал (ненулевой код) → kill + перезапуск. «Выход» из меню (код 0) → супервайзер останавливается. Запускать через `./run.sh` | — | ✅ |
| 49 | Фриз: спиннер завис (повтор) | ⏳ Спиннер/main-поток периодически зависает. Прошлый фикс не закрыл всё. **Авто-сбор трейса:** `run.sh` при детекте фриза (heartbeat) теперь **сам снимает `sample` зависшего процесса** в `~/Library/Application Support/VoiceWheel/freeze-<дата>.txt` ДО перезапуска — юзеру ловить не надо, после фриза просто прислать файл. Жду первый трейс для диагноза. Кандидаты: GIL во время тяжёлого STT/LLM, live-apply на main-потоке | 48 | ⬜ |
| 50 | Триггеры применять без рестарта | ✅ Раньше смена триггера применялась только после ручного рестарта. Теперь `save_` сравнивает старый/новый конфиг и **если изменились триггеры / STT-движок-модель / язык — приложение перезапускается само** (переиспользует `os.execv`-механизм из #43), что чисто применяет их. Выбрал авто-рестарт вместо live-перерегистрации event-tap'а: тот трогает фриз-чувствительный Quartz-tap на отдельном потоке (риск). Только-live настройки (LLM/модели/голос/concurrent) применяются без рестарта, как и было. Сравнение триггеров по сигнатуре (legacy `{kind,key}` == список). ⚠️ Рестарт укреплён: путь проекта содержит двоеточие, а `os.pathsep` == `:` — PYTHONPATH с таким путём ломался. Ре-эксек теперь через `python -c` с `sys.path.insert(src)` + `runpy.run_module` (двоеточие-безопасно, не зависит от cwd/способа запуска). Проверено: бутается даже из env без PYTHONPATH | 32, 43 | ✅ |
| 51 | Сброс на стандартные (на вкладку) | ✅ Одна кнопка **«Сброс»** в нижней панели (слева от «Сохранить», в общей зоне) — сбрасывает **активную** вкладку к дефолтам из dataclass'ов (`LLMConfig`/`STTConfig`/`TTSConfig`/`HotkeyConfig`; на «Язык» — к системному). LLM-сброс ещё удаляет правила-оверрайды + чинит видимость строки модели и «+ Добавить». Не пишет конфиг (Save загорается зелёным) — закрыть окно = откат. Изначально кнопки были на каждой вкладке, перенёс в нижнюю панель по фидбэку. Save армится **только если сброс реально что-то поменял** (сравнение снимка вкладки до/после) — сброс уже-дефолтной вкладки не активирует Save (фикс по фидбэку) | 35 | ✅ |
| 53 | Отдельная вкладка «Язык» | ✅ Пятая вкладка **«Язык»** (🌐): дропдаун Русский/English + предупреждение «после Сохранить приложение перезапустится». Выбор пишется в `ui_language` на «Сохранить»; если язык изменился → рестарт (см. #43). Реализована вместе с #43 | 35, 43 | ✅ |
| 56 | Save: состояние «есть несохранённое» | ✅ Вместо текстовой ноты «Сохранено и применено…» — **сам Save отражает состояние**: заблокирован, пока нет изменений; на любое изменение (попап/поле/чекбокс/захват/правило/сброс) загорается **зелёным** и доступен; после сохранения — снова заблокирован. Инфо «после перезапуска» переехало в постоянные подсказки на вкладках Речь/Триггеры. По фидбэку (нота обрезалась) | 35 | ✅ |
| 54 | Хоткеи: клавиатура + мышь раздельно | ✅ У каждого триггера (колесо + озвучка) **две строки**: клавиатура (комбо) и мышь (вид+кнопка), у каждой свой «Поймать» (клавиатурный ловит только клавиши, мышиный — только мышь). **Обе активны одновременно** (по выбору юзера). В конфиге `hotkey` стал **списком** привязок (`[{kind,key},…]`); старый одиночный `{kind,key}` мигрирует на лету. `TriggerManager` уже принимал список → регистрирует все. Пустая строка = привязки нет (тачбук без мыши → юзай клавиатуру). ⚠️ Авто-блокировка мышиной строки при отсутствии мыши — **отложена** (#57): в этой сборке pyobjc нет IOKit HID, надёжного детекта железа нет; пока строка просто опциональна (пусто = выкл) + подсказка | 35, 50 | ✅ |
| 29 | Превью голоса при смене в настройках | При смене голоса в дропдауне — сразу проигрывается короткий пример этим голосом («Привет! Это пример выбранного голоса.»), предыдущий обрывается. Для Piper качает голос, если ещё нет (с нотой). Работает для Piper и macOS | TTS | ✅ |
| 30 | Триггеры v2: захват клавиш (+ комбо) | Кнопка **«Поймать»** у триггера записи и озвучки: нажал нужную клавишу/комбо (вроде ⌘F) → записалось как `cmd+f` и подставилось в поле (kind→keyboard). Бэкенд триггеров теперь понимает модификаторы (hold-комбо), а не только одиночную клавишу. Применяется после рестарта (триггеры не live). Детект кнопок мыши вынесен в #34 | 27 | ✅ |
| 34 | Триггеры: «Поймать» ловит кнопки мыши | Кнопка «Поймать» теперь ловит и кнопки мыши: нажал правую/среднюю/боковую → подставилось (`mouse` right/middle или `mouse_side N`). Esc — отмена. HID-enum заменён на click-capture (надёжнее enum'а устройств). Оговорка: уже занятую боковую кнопку наш event-tap глушит — для неё число вписать вручную | 30 | ✅ |
| 31 | Настройки: «Озвучка включена» выше + дизейбл | Чекбокс «Озвучка включена» поднят в начало блока «Голос», над выбором голоса и кнопкой озвучки; пока выключено — голос, кнопка премиум-голосов и триггер озвучки **задизейблены**. Триггер записи (колесо) вынесен в свой блок | 27 | ✅ |
| 32 | Применять настройки без перезапуска | На «Сохранить» контроллер пересобирает **дешёвые** сервисы сразу: LLM-клиент + per-prompt модели, голос (TTS), тумблер concurrent — без рестарта. Триггеры (event-tap) и STT-модель остаются на рестарт (перерегистрация tap'а / перезагрузка Whisper — тяжело/рискованно). Нота в окне честно говорит, что применилось сразу, а что после рестарта | 19 | ✅ |
| 33 | Настройки: модель на каждый промпт (UI) | Блок «🎛 Модель на промпт»: дропдаун промпта (из `prompts/`) + бэкенд/модель для него. «(как базовая)» = берёт движок/модель из блока «Обработка речи» (базово для всех). Переключаешься между промптами — правки каждого запоминаются (edit-buffer), на «Сохранить» пишутся в `sector_models`. Бэкенд (`sector_models` с фолбэком) уже был | 19 | ✅ |
| 35 | Настройки на вкладках | Окно переросло (Save не виден) → разбито на табы **LLM / Речь / Голос / Триггеры** (NSTabView, flipped-вью). Кнопка «Сохранить» и нота вынесены под вкладки — всегда видны. Окно стало компактным (520×440) | 27 | ✅ |
| 44 | Вкладка «Речь»: понятнее | STT-**модель** — теперь дропдаун (tiny/base/small/medium/large) вместо ручного ввода; **движок** и **язык** получили поясняющие подсказки (что выбираем, на что влияет). `auto` в языке — определяет по речи (поднят первым). Тултипы на каждый контрол — отдельно в #41 | 35 | ✅ |
| 47 | Очистка процессов при выходе | При выходе/крэше приложение убивает за собой всё, что подняло: прогретый `claude`-подпроцесс + останавливает аудио. Хуки: меню «Выход» (`applicationWillTerminate_`) и нормальный выход/исключение (`atexit`). ⚠️ Частично: `pkill -9` (SIGKILL) поймать нельзя; SIGTERM в Cocoa-цикле ненадёжен — там спасает супервайзер (#48) | — | ✅ |

| 57 | Мышь нет → блокировать мышиную строку | ➖ Закрыто через #58: вместо хрупкого авто-детекта железа (нет IOKit) — **ручная галочка вкл/выкл** на каждой строке. Нет мыши → снял галочку «Мышь», строка блокируется. Проще и надёжнее авто-детекта | 54 | ➖ |
| 58 | Триггеры: галочка вкл/выкл на строку + защита от «без триггера» | ✅ У каждой строки триггера (колесо/озвучка × клавиатура/мышь) — **галочка-заголовок**: вкл = строка активна, выкл = поля **заблокированы** и привязка не сохраняется. Загрузка ставит галки по наличию привязок; save пишет только включённые. **Фикс F1:** сохранить колесо без единого триггера теперь нельзя — блок + нота «Включи хотя бы один». TTS-строки дополнительно гейтятся мастер-чекбоксом озвучки. Заодно: клик по пустому месту вкладки снимает фокус с инпута (фикс «фокус не уходит») | 54 | ✅ |

| 59 | Мышь: дружелюбный селектор кнопки | ✅ Вместо `[вид ▾][номер]` — один список понятных названий: Правая / Средняя / Боковая 3 (назад) / Боковая 4 (вперёд) / Боковая 5. Index-mapped на `(kind,key)`; «Поймать» нестандартной кнопки (напр. №6) добавляет пункт «Боковая 6» и он round-trip'ится. Заранее перечислить кнопки мыши нельзя (нет IOKit/HID) — поэтому курируемый список + «Поймать» | 58 | ✅ |

| 60 | Save = форма vs сохранённое + UX-мелочи | ✅ **Dirty-модель переписана:** Save отражает не «было ли действие», а **отличается ли форма от сохранённого** (снимок всей формы при load = baseline, на любое изменение пересчёт). Теперь «Сброс» к уже-сохранённому состоянию и change-and-revert **гасят Save** (фикс репорта). + Вкладка «Язык»: убрано дублирование подписи (дропдаун прямо под заголовком). + После рестарта по «Сохранить» окно настроек **снова открывается на той же вкладке** (маркер-файл `reopen_settings`) | 51, 56, 58 | ✅ |

| 61 | Аудит F2/F4/F5: ключ и провайдер | ✅ **F2:** смена провайдера (Anthropic↔OpenAI) больше не теряет модель — память модели на каждого провайдера (`_model_by_provider`), переключение туда-обратно восстанавливает. **F4:** API-ключ маскируется (`NSSecureTextField`) + чекбокс «Показать» (плейн-зеркало). **F5:** сохранение «Прямого API» с пустым ключом — **предупреждение** «Ключ не задан — облачный LLM работать не будет» (не блокирует, save проходит) | 36, 38 | ✅ |

| 62 | Глобальное предупреждение о рестарте | ✅ Раньше «применится после перезапуска» жило **внутри вкладок** (Язык/Триггеры/Речь) — изменил там, ушёл на другую вкладку, сохранил → рестарт без предупреждения. Теперь это **одна всегда-видимая плашка** над кнопками (вне вкладок), оранжевая, показывается **динамически** только когда несохранённые изменения реально требуют рестарта (`_restart_pending` = язык/триггеры/STT ≠ сохранённому). Пер-табные подсказки убраны; раскладка вкладок ужата на 22px под плашку | 50, 60 | ✅ |

| 63 | Не переспрашивать выданный доступ | ✅ Иногда переспрашивал Accessibility, хотя выдан — особенно после авто-рестарта по «Сохранить» (TCC лагает, сразу после ре-эксека отвечает «не доверен»). Фикс: `_ensure_accessibility` теперь **сначала тихо проверяет** (prompt=False) и показывает диалог только если реально нет доступа; на рестарте — `allow_prompt=False` (через env `VW_RESTARTED`), чтобы не переспрашивать. ⚠️ Полный фикс флакости грантов (mic/input monitoring тоже) — подписанный `.app`-бандл (грант на бинарь python хрупкий); отдельная задача | 50 | ✅ |

| 64 | Код-ревью: чистка и дедуп | ✅ Полный код-ревью (3 агента: мёртвый код / дублирование / архитектура). Удалён мёртвый код: `change_count` + restore-стек в `clipboard.py`, `reload_sectors` в `modes.py`, константа `KINDS` и 7 неиспользуемых i18n-ключей. Дедуп: `.env`-хелперы (`read_env`/`write_env_key` в `core/config.py`, переиспользованы в `settings` и `_load_dotenv`), предикат рестарта (`_restart_changed` — один источник правды для `_restart_pending` и `save_`). **Фикс H1:** дефолт TTS-движка хардкодился как `system`/`ru_RU-irina-medium` вместо дефолтов `TTSConfig` (Piper). Границы core↔platform чистые, циклов нет. compile + ruff + 58 тестов зелёные | — | ✅ |

| 65 | LLM/Речь: UI-полиш по фидбэку | ✅ **LLM:** пустая кнопка-действие Ollama (`_ollama_action`) больше не висит серой коробкой слева от «Проверить» — скрыта по умолчанию, появляется только с реальным текстом (Установить/Запустить); заодно ушёл лишний отступ. **Речь:** разбор движков (auto/mlx/faster-whisper) и моделей (tiny…large) переписан по пунктам — что выбрать и на что влияет (было однострочно и куце) | 44, 62 | ✅ |

| 66 | Один экземпляр приложения | ✅ Запуск второго/третьего раза больше не плодит дубликаты (лишний значок в меню-баре + конкурирующий event-tap). При старте берём эксклюзивный `flock` на `voice_wheel.lock` и держим fd весь жизненный цикл — ОС снимает блокировку сама при смерти процесса (даже на SIGKILL), без возни со stale-PID. Второй запуск видит занятый лок → печатает «Voice Wheel уже запущен…» и выходит. Само-рестарт (`os.execv`) не страдает: fd — close-on-exec (дефолт Python), при exec лок снимается и ре-эксек берёт его заново. `run.sh` дополнен `pkill -9` после SIGTERM — Cocoa-апп ненадёжно реагирует на SIGTERM, добиваем, чтобы лок освободился перед свежим стартом | 47, 48 | ✅ |

| 67 | Предупреждение о рестарте — всегда сверху | ✅ Раньше плашка «применится после перезапуска» жила над кнопками снизу, появлялась динамически и **обрезалась** в одну строку. По фидбэку: теперь это **постоянная плашка сверху, над вкладками** — пользователь сразу видит, какие настройки стоят рестарта. Текст всегда-истинный («язык / триггеры / STT применяются после перезапуска») и **переносится на 2 строки**. Плашка **полностью статична** — один и тот же текст и начертание всегда (пробовал жирнить при ожидающем рестарте — жирный шире, вылезал и обрезался, да и сам факт «подсказка меняется» был неудобен → убрал). Низ окна: старый отступ под нижнюю плашку убран (вкладки опущены, зазор 28→12px), окно выросло всего на 24px (440→464) | 62 | ✅ |

| 68 | Удобный клавиатурный хоткей по умолчанию (выкл.) | ✅ Строки клавиатурного триггера теперь **предзаполнены** удобными комбо: колесо/запись — **⌃⌘Z** (`cmd+ctrl+z`), озвучка — **⌃⌘X** (`cmd+ctrl+x`). По умолчанию **галочка снята** → строка заблокирована, привязка не сохраняется и не активна, пока пользователь не включит (активный дефолт остаётся боковая кнопка мыши). Комбо подобраны под hold-to-record: левая рука (правая свободна для тачпада), **не печатают текст** (⌃ глушит ввод, ⌘ держит символ стабильным для pynput-матчинга) и не конфликтуют со стандартными шорткатами macOS. Включил галочку — комбо уже готово, либо перепоймал своё кнопкой «Поймать». Реальная привязка в конфиге переопределяет подсказку | 54, 58 | ✅ |

| 69 | Выгрузка локальной модели при выходе | ✅ Раньше после закрытия приложения локальная модель ollama висела в памяти/GPU ещё ~30 мин: мы пиним `keep_alive: 30m` (чтобы не было перезагрузок между записями), но это переживало выход — пользователь видел «вторую оламу» (это дочерний `runner`, ~5 ГБ GPU). Теперь на **реальном выходе** (меню «Выход» / Ctrl+C / краш — `_cleanup_on_quit` + `atexit`) шлём ollama `keep_alive: 0` → модель и её процесс-раннер сразу выгружаются. При **само-рестарте** по «Сохранить» (`_cleanup`) модель НЕ выгружаем — остаётся тёплой. Демон `ollama serve` не трогаем — это сервис пользователя (brew/launchd), поднят до приложения и не наш. +3 теста (мок requests) | 47, 32 | ✅ |

| 70 | Гарантированная очистка процессов при любом выходе | ✅ Аудит всего, что приложение порождает, + железная гарантия: после выхода ничего не остаётся. **Лик-поверхность** = только дочерние subprocess (тёплый `claude`, разовые CLI-вызовы, `ollama serve`); потоки/таймеры/event-tap — in-process, умирают с процессом. **(1)** Приложение ловит `SIGTERM`/`SIGHUP` → graceful teardown (выгрузка ollama-модели, убийство claude, стоп аудио) — раньше SIGTERM жёстко убивал python без очистки. **(2)** `run.sh` запускает приложение в **своей process-group** (`set -m`) и на стопе/рестарте/фризе бьёт **всю группу** (TERM→KILL) — приложение + все его дети, как бы оно ни умерло (вкл. SIGKILL/фриз, которые не перехватить). PGID пишется в файл; если прибили сам `run.sh` — на следующем старте добиваем осиротевшую группу. **(3)** Вариант 1: `run.sh` владеет ollama — если поднял её сам (нет brew), глушит на выходе (раннер умирает с `serve`); внешний brew-`serve` не трогаем (модель уже выгружена приложением). **(4)** `claude_warm`: `terminate`→`kill` фолбэк. Проверено вживую: SIGTERM приложения / стоп `run.sh` / краш-рестарт (код 137→1 экземпляр) — **0 выживших**, модель выгружена | 47, 48, 69 | ✅ |

| 71 | Промпты видны в настройках + доступ к папке | ✅ Фидбэк: добавил новый промпт в папку — нигде не появился, «непонятно как это работает». На вкладке LLM — новый блок **«🧩 Промпты — секторы колеса»**: живой список текущих промптов (читается **свежим из папки**, а не из кэша — поэтому только что добавленный файл виден сразу при открытии настроек), кнопки **«📂 Открыть папку промптов»** (Finder) и **«Обновить»** (перечитать без перезапуска). Подсказка объясняет: файл `N-Название.md` = сектор колеса, применение к колесу — после перезапуска. Раньше секция «Модель на промпт» вообще не показывала список файлов. Причина исходного «бага»: файла не было в той папке + кэш секторов на процесс (нужен рестарт колеса) | 6, 33 | ✅ |

| 72 | Вкладка «Модели» — загрузка и управление (Ollama) | ✅ Отдельная вкладка **«Модели»** (по выбору) — единое место для скачивания/управления локальными моделями. **Живой статус** Ollama с версией («✅ Ollama запущена · v0.24.0» — теперь видно, что реально запущено) + кнопки **«Перезапустить Ollama»** (#9), «Проверить», установить/запустить. **Список установленных** моделей с размерами (видно, что скачано: qwen2.5:3b 1.9 GB и т.п.). **Скачивание** (#8): предложенные модели + своё имя; понятные ⏳/✅/❌ в строке статуса; кнопка блокируется на время загрузки; по готовности модель появляется в списке и в выборе. Ссылка на библиотеку моделей Ollama. На вкладке **LLM** убраны дублирующие «Скачать» + блок статуса → осталась только выборка модели + указатель на «Модели». Удалены 3 осиротевших i18n-ключа. По фидбэку при тестировании добил: **(а)** реальный **прогресс загрузки** — pull теперь стримится через `/api/pull`, в строке статуса живой «⏳ «модель» · 45% (2.1/4.7 GB)»; **(б)** на вкладке LLM выпадашка модели Ollama показывает **только установленные** (не предлагает то, что не скачано); **(в)** нижняя строка-статус **переносится на 2 строки** (длинные сообщения не обрезались); **(г)** контролы вкладки LLM сужены, чтобы не залезали **под скроллбар** (после добавления блока промптов вкладка стала прокручиваться и 300px-комбо уезжали под скролл). Проверено вживую (статус с версией + список установленных) | 8, 9, 36, 40 | ✅ |

| 73 | Вкладка «Промпты» + селектор модели в правилах (#10) | ✅ По фидбэку промпты вынесены на **отдельную вкладку «Промпты»**: счётчик **«Сейчас промптов: N»** (без перечисления имён — количества достаточно), «📂 Открыть папку промптов», «Обновить»; туда же переехала секция **«Модель на промпт — правила»**. **#10 закрыт:** модель в правиле теперь **editable-combo-селектор**, а не свободный инпут — выпадашка показывает **установленные** модели для Ollama (или списки провайдера для API/CC) и **переключается при смене движка** в правиле, как основной блок LLM. Окно расширено **520→580** под 7 вкладок и строки правил. Вкладка LLM разгрузилась — осталась только настройка подключения. **+ Удаление моделей:** список «Установлено» стал строками с кнопкой **✕** у каждой модели (с подтверждением, `ollama rm`) — освободить место от ненужной/ошибочно скачанной; после удаления список и селекторы обновляются | 6, 10, 33 | ✅ |

| 74 | LLM: модель Ollama — чистый селектор (не инпут) | ✅ По фидбэку: на вкладке LLM поле «Модель» для Ollama было **editable-combo** (можно было печатать), хотя там нужно просто выбирать **уже скачанную**. Заменил на **`NSPopUpButton`** — чистый дропдаун без ввода, наполняется живым списком установленных моделей (из `/api/tags`). Скачивать/удалять — на вкладке «Модели». Настроенная модель остаётся выбираемой, даже если её удалили (конфиг не меняется молча). Save корректно реагирует на смену; экшен попапа срабатывает только на ручной выбор → нет фантомного dirty при async-обновлении списка. **Заодно сократил подписи движков** в правилах (по фидбэку): «Локально — Ollama (бесплатно, без ключа)» → **«Ollama»** (Ollama может быть и удалённой — URL настраивается), «Claude Max (подписка)» → **«Claude (подписка)»** (работает на любой подписке, «Max» лишнее); «(нужен ключ)» у API-вариантов оставил | 36, 72 | ✅ |

| 75 | Ollama — убрать «локально» (может быть удалённой) | ✅ Ollama не обязана быть локальной — URL настраивается на свой удалённый сервер. Поэтому везде убрал/смягчил «локально»: радиокнопка подключения «Ollama (локально, бесплатно)» → **«Ollama (бесплатно)»**; подсказка к URL → «Адрес Ollama — локальной или удалённой (свой сервер)»; подсказка движка «Локальная модель через Ollama…» → «Модель через Ollama… URL — локальный или свой сервер»; онбординг — то же. Удалённая Ollama уже работает (просто меняешь URL); токен-авторизация для платного хостинга — отдельная опция на потом | 36, 74 | ✅ |

| 76 | Токен-авторизация для удалённой Ollama | ✅ Опция №3: для удалённой/хостовой Ollama за авторизацией — поле **«Токен (опц.)»** в блоке Ollama (маскированное, хранится в `.env` как `OLLAMA_API_KEY`). Если задан, все запросы к Ollama шлют `Authorization: Bearer <token>`: обработка речи (`core/llm.py` — chat / reachable / unload) и UI (статус `/api/tags`+`/api/version`, скачивание `/api/pull`). Локальной Ollama не нужен (пусто = без заголовка). Применяется **сразу** (как API-ключи), без рестарта; учитывается в dirty/Save. +тесты (заголовок шлётся при токене, пусто — без) | 36, 75 | ✅ |

| 77 | Радио подключения — порядок и короткие подписи | ✅ По фидбэку переработал 3 радио типа подключения: порядок **Ollama · Claude Code CLI · Прямой API** (Ollama первой, по дефолту); подписи укорочены — «Прямой API (облако, по ключу)» → **«Прямой API»**, «Ollama (бесплатно)» → **«Ollama»**, «Claude Code (CLI, подписка Max)» → **«Claude Code CLI»**. Дефолт-выбор переведён на tkey (не индекс), логика типов не тронута. **+ Авто-фокус инпута при открытии вкладки убран:** NSTabView при выборе вкладки фокусировал первое текстовое поле (на «Модели» — комбо «Скачать модель»), теперь по `tabView:didSelectTabViewItem:` и при открытии окна фокус сбрасывается на окно — каретка не лезет в инпут сама. **+ «Нет моделей — скачать»:** если Ollama запущена, но не скачано ни одной модели, селектор модели на LLM скрывается и вместо него кнопка **«⚠️ Нет моделей — открыть „Модели"»** (перекидывает на вкладку «Модели»). Раньше показывалась удалённая/настроенная модель, которой уже нет | 36, 75 | ✅ |

| 78 | Вкладка «Ключи» — централизованное хранилище секретов | ✅ Фундамент эпика «Подключения v2». Новая вкладка **«Ключи»** (4-я): все секреты в одном месте — **Claude (Anthropic)**, **OpenAI**, **Ollama (токен)**, каждый маскированный + «Показать», хранятся в `.env` (gitignored), применяются сразу. Поля ключей **уехали с вкладки LLM** (группа «Прямой API» оставила провайдер+модель + указатель на «Ключи»; Ollama-токен ушёл из группы Ollama). Load читает все ключи из `.env` в строки; save пишет все обратно (пустой ключ → строка удаляется); dirty/Save учитывает изменения (снимок `tab_keys`). Предупреждение «нет ключа» при сохранении «Прямой API» читает ключ текущего провайдера из вкладки. Окно расширено **580→640** под 8 вкладок. Удалены осиротевшие i18n. Проверено: round-trip ключей через `.env` (write+reflect+read), структура, dirty, show-toggle. Дальше: #13 (правило=полный интерфейс, выбор только доступных) | 36, 38 | ✅ |

| 79 | Выбор только доступных подключений + редирект на «Ключи» (13a) | ✅ На основе вкладки «Ключи»: предлагаются только подключения, у которых есть ключ. **Прямой API:** провайдер выбирается только тот, у кого введён ключ (другой — задизейблен); если ключа нет ни у кого — вместо провайдера/модели кнопка **«⚠️ Нет ключа — добавить на «Ключи»»** (перекидывает на вкладку). **Правила:** в дропдауне движка варианты без ключа (Claude API / OpenAI API без ключа) **задизейблены** (Ollama/Claude Code/CLI — всегда, ключ не нужен). Живое обновление: ввёл/стёр ключ на «Ключи» → доступность сразу пересчитывается (база + все правила). Это часть #13; осталось 13b — сворачиваемые панели правил (полный интерфейс как база) | 36, 78 | ✅ |

| 80 | Правила = сворачиваемые панели (13b, #13 закрыт) | ✅ Каждое правило «модель-на-промпт» теперь **сворачиваемая панель**: шапка = ▸/▾ + сводка **«промпт → движок · модель»** + ✕; под ней раскрывается детальный ряд с полным выбором ([промпт ▾] [движок ▾] [модель ▾]), как база. Загруженные правила — **свёрнуты** (видна только сводка), новое по «+ Добавить» открывается развёрнутым для настройки; клик по ▸/▾ — развернуть/свернуть. Сводка обновляется в реальном времени при любом изменении. Вместе с 13a (выбор только доступных движков) задача **#13 закрыта** | 10, 79 | ✅ |

| 81 | Ключи: кнопка-глаз + провайдеры фильтруются по ключу | ✅ По фидбэку: **(1)** «Показать» на вкладке «Ключи» — больше не чекбокс, а **кнопка-иконка глаза** (без текста). Клик — показать токен; показ держится **только пока активна вкладка «Ключи»** — сменил вкладку (или закрыл/переоткрыл окно) → снова маска, нужно нажать глаз заново (`_mask_all_keys` по `tabView:didSelectTabViewItem:` + на load). **(2)** На вкладке LLM в «Прямой API» провайдер теперь **фильтруется** (раньше дизейблился): в списке только те, у кого заполнен ключ; нет ключа — выбор модели скрыт; ни одного ключа — кнопка-редирект «Нет ключа — добавить на «Ключи»» | 78, 79 | ✅ |

| 82 | Вкладка «Логи» + статус ключа «не работает» (эпик закрыт: #15 + #14) | ✅ Новая 9-я вкладка **«Логи»**: простой человекочитаемый журнал того, что приложение делало — свежие сверху, **сам обновляется** (NSTimer 1.5с, тикает только пока вкладка видна), кнопка **«Очистить»**. Форматы строк: 🎤 Диктовка → «…»; 🎤 Записано → 🧠 <промпт> → «…»; 📋 Буфер был · 🎤 Записано → 🧠 <промпт> → «…»; 🔊 Озвучено: «…»; ⚠️ Ошибка: <человеческая категория> — <сообщение>; 💥 Сбой. Хранилище — потокобезопасный `core/event_log.py` (кольцо на 300 событий + зеркало в `events.jsonl`, переживает рестарт; краши ловят `sys`/`threading.excepthook`). **#14:** на вкладке «Ключи» красный бейдж **«⚠️ ключ не работает»** появляется после первого запроса с ошибкой авторизации для бэкенда этого ключа (`LLMClient.backend_for` тегирует событие точным бэкендом → `_BACKEND_TO_KEY`); снимается сам при следующем успешном запросе или как только пользователь начал править ключ. Окно расширено до 680×520 под 9 вкладок и поле журнала | 12, 13, 79 | ✅ |

| 83 | Формат передачи в LLM: разделяем «мою речь» и «буфер» | ✅ Транспорт теперь метит входы ровно теми метками, на которые опираются промпты: **`ГОЛОС:`** — моя речь (намерение), **`БУФЕР:`** — текст из буфера (может быть чужим). Без контекста уходит только `ГОЛОС:`; с контекстом — `ГОЛОС:` (ведёт) + `БУФЕР:` (следом, как материал). Решает проблему «модель принимает чужое сообщение из буфера за моё». System-промпт = **тело промпта как есть** (его BASE-секция сама задаёт формат вывода и режимы) — код больше не подмешивает свой префикс/хвост (`_CONTEXT_PREFIX`/`_OUTPUT_TAIL` убраны); для transform и context system одинаков (бонус: prompt caching переиспользует). Дефолтные промпты (сид пустой папки) переведены на ту же структуру BASE+SECTOR. Поддержан режим «только БУФЕР» (молча взял буфер) | 4, 13 | ✅ |

**Сделано: 83 ✅ · 2 ➖ убрано · 1 ➗ разбита · Очередь: 1 ⬜ · эпик «Подключения v2» ЗАКРЫТ: ✅Ключи → ✅Правила(#13) → ✅Статус-ключа(#14) → ✅Логи(#15)** (#49 фриз — жду файл) · Навигация: вкладки + шире окно · ⏳ на подумать: `.app`-бандл (стабильные гранты) · Архив: Windows-порт, инсталлеры, Linux

### Подтверждено в ходе работы (не отдельные фичи)
- **Промпты полностью data-driven** (#6 из обсуждения): колесо делится по числу `*.md` в `prompts/`; добавил/удалил файл → изменилось число секторов (проверено: 4↔5). Код **не ссылается** на конкретные промпты — удалён мёртвый `DefaultMode` (единственная ссылка на слаг `normalize`). `DEFAULT_PROMPTS` в `modes.py` — только сид для пустой папки.
- **Фикс спиннера/импульса**: `JobTracker` переведён со счётчика на набор токенов — отменённая (superseded) задача при позднем завершении больше не гасит спиннер новой записи.

## 🗄 Архив (позже)

| № | Фича | Описание | Статус |
|---|------|----------|--------|
| 16 | Кроссплатформа: Windows | Оверлей / хоткей / буфер / трей под Windows. Отложено: без Windows-машины для теста выйдет плохо — вернёмся, когда будет где гонять | 🗄 |
| 21 | Инсталлеры (Mac / Windows) | Распространяемый/подписанный установщик. Локальный double-click `.app` для Mac уже есть; подписанный Mac-инсталлер требует Apple Developer ID, Windows-инсталлер зависит от #16. Отложено вместе с Windows | 🗄 |
| 22 | Кроссплатформа: Linux | Оверлей и пр. под Linux (сложнее из-за Wayland). Отложено — вернёмся позже | 🗄 |
